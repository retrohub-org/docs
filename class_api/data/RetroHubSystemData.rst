.. include:: /global/godot_api.rst

.. _api_RetroHubSystemData:

RetroHubSystemData
==================

Represents a system from the user's library. Holds all informations available about a system. It is generated by :ref:`api_RetroHub` when the app in launched, and passed to themes through the :ref:`system_received <api_RetroHub_system_received>` signal.

The received object is unique and shared among the app. An instance can also be used to uniquely identify this system, which is done for other parts of the API.

Enumerations
------------

.. _api_RetroHubSystemData_Category:

enum **Category**:

- **Console = 0** - A retro console.
- **Arcade = 1** - An arcade machine.
- **Computer = 2** - A retro computer.
- **GameEngine = 3** - A game engine.
- **ModernConsole = 4** - A modern console (7th generation upwards).

Properties
----------

.. list-table::
	:widths: 20 70 10
	:header-rows: 1

	* - Type
	  - Name
	  - Default
	* - |godot_string|
	  - :ref:`name <api_RetroHubSystemData_name>`
	  - *other*
	* - |godot_string|
	  - :ref:`fullname <api_RetroHubSystemData_fullname>`
	  - ``""``
	* - |godot_string|
	  - :ref:`platform <api_RetroHubSystemData_platform>`
	  - ``""``
	* - :ref:`Category <api_RetroHubSystemData_Category>`
	  - :ref:`category <api_RetroHubSystemData_var_category>`
	  - ``0``
	* - |godot_int|
	  - :ref:`num_games <api_RetroHubSystemData_num_games>`
	  - ``""``

----

.. _api_RetroHubSystemData_name:

	|godot_string| **name** = ``"..."``

The system's "short" name. This field is always set, and uniquely identifies this system.

----

.. _api_RetroHubSystemData_fullname:

	|godot_string| **fullname** = ``""``

The system's full name.

----

.. _api_RetroHubSystemData_platform:

	|godot_string| **platform** = ``""``

The system's platform. While usually the same as the :ref:`name <api_RetroHubSystemData_name>` field, it can be different for systems that are very similar to a base system, such that RetroHub uses the same configuration among them. For example, Nintendo 64 DD on Nintendo 64 and Sega Genesis CD on Sega Genesis.

----

.. _api_RetroHubSystemData_var_category:

	:ref:`Category <api_RetroHubSystemData_Category>` **category** = ``0``

The system's category.

----

.. _api_RetroHubSystemData_num_games:

	|godot_int| **num_games** = ``0``

The number of games detected for this system. In practice, this will always be larger than 0, as RetroHub doesn't send system data if there are no games for it. This can be used to known in advance how many game datas will arrive for that specific system.

Methods
-------

.. list-table::
	:widths: 20 80
	:header-rows: 1

	* - Type
	  - Name
	* - :ref:`Category <api_RetroHubSystemData_Category>`
	  - :ref:`category_to_idx <api_RetroHubSystemData_category_to_idx>`
	* - |godot_string|
	  - :ref:`idx_to_category <api_RetroHubSystemData_idx_to_category>`

----

.. _api_RetroHubSystemData_category_to_idx:

	:ref:`Category <api_RetroHubSystemData_Category>` **category_to_idx** ( |godot_string| category )

Converts a category name to the enum value. Used internally.

----

.. _api_RetroHubSystemData_idx_to_category:

	|godot_string| **idx_to_category** ( :ref:`Category <api_RetroHubSystemData_Category>` category )

Converts a category enum value to a string name. Used internally.